<div class="mt-24 mx-auto w-full xl:w-4/5 px-4 sm:px-6 xl:px-8" id="review-insight">
  <div class="flex flex-wrap">
    <div class="w-full lg:flex-1 flex">
      <%= live_redirect to: "/insights/cma/#{@insight_uuid}/create" do %>
        <div class="pr-2 mr-2 border-r border-gray-400 flex h-full justify-center items-center cursor-pointer">
          <svg class="w-6 h-6 text-gray-400" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
          </svg>
        </div>
      <% end %>
      <div class="flex-1">
        <div>
          <h4 class="text-gray-400">Step 2 of 3</h4>
          <h3 class="mt-1">Review and add quick adjustments</h3>
        </div>
        <p class="text-sm w-full lg:w-3/5">
          <strong>Next step:</strong> Lastly you can organize and preview your final document.
          <span class="bg-yellow-200">The adjustments made here are intended either as a starting point or for quick publication.
          You can edit or fine tune any information in the next step.</span>
        </p>
      </div>
    </div>
    <div>
      <%# template selector OR/AND adjustments %>
    </div>
  </div>
  <div class="mt-8 grid grid-cols-1 lg:grid-cols-3 gap-6" id="review-subject">
    <div class="col-span-2 p-4 bg-cyan-50 rounded-lg shadow">
      <%= live_component @socket, SubjectForm,
        label: "Review property accuracy",
        subject: @subject,
        id: :subject_adjustments
      %>
    </div>
    <%= for comp <- @selected_comps do %>
      <div class="bg-bgray-200 rounded-lg relative overflow-hidden shadow">
        <%= if comp do %>
          <div class="rounded-t-lg relative h-24 overflow-hidden bg-bgray-900 ">
            <img src="<%= comp.main_photo_url %>" alt="" class="absolute h-full w-full object-cover grayscale-60 opacity-80" />
            <div class="absolute h-full w-full top-4 left-4 deep-text-shadow">
              <h4 class="text-white shadow"><%= comp.street_number %> <%= comp.street_name %></h4>
            </div>
          </div>
        <% end %>
        <div class="p-4">
          <h4 class="font-bold text-sm">Adjustment considerations</h4>
          <p class="text-sm"><%= Utilities.summarize_sale_info(comp) %></p>
          <ul class="mb-2 flex flex-wrap -mx-2">
            <%= for adj <- Enum.map(Enum.filter(comp.adjustments, fn {_, v} -> Map.has_key?(v, :to_text) end), fn {_, v} -> v end) do %>
              <li class="w-1/2 py-1 px-2 text-sm"><%= adj.to_text %></li>
            <% end %>
          </ul>
          <%= if Map.has_key?(comp.adjustments, :sqft) do %>
            <h4 class="font-bold text-sm">Auto Adjustments</h4>
            <p class="text-sm"><%= Utilities.summarize_auto_adjust(comp.adjustments.sqft) %></p>
          <% end %>
          <%# template selector %>
          <%# adjustment slider %>
        </div>
      </div>
    <% end %>
    <div class="p-4 bg-cyan-50 rounded-lg shadow">
      the end
      <%# continue with auto adjust %>
      <%# continue without adjustments (I got this.) %>
    </div>
  </div>
</div>
